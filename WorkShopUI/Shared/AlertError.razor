@if (Display)
{
    <Alert Color="Color.Warning" @bind-Visible="@Display">
        <AlertMessage>
            @Message
        </AlertMessage>
    </Alert>                        
}

@code {
    [Parameter]
    public string Message { get; set; }

    [Parameter]
    public bool Display { get; set; }
}
